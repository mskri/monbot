import { hello } from './hello';
import { channelId } from './channelId';
import { emojiId } from './emojiId';
import { roleId } from './roleId';
import { adminOnly } from './adminOnly';

export const commands = [hello, channelId, emojiId, roleId, adminOnly];
